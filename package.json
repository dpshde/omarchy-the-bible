{
  "name": "omarchy-route-bible",
  "version": "1.0.0",
  "private": true,
  "description": "Omarchy plugin for parsing a passage, selecting verses, and opening route.bible.",
  "type": "module",
  "packageManager": "pnpm@10.28.2",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dpshde/omarchy-route-bible.git"
  },
  "scripts": {
    "build": "node scripts/bundle-grab-bcv.mjs && node scripts/bundle-bible.mjs && node scripts/bundle-route.mjs",
    "fetch-bsb": "node scripts/fetch-bsb.mjs",
    "install:local": "node scripts/install-local.mjs",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build",
    "lint": "pnpm exec oxlint src tests scripts",
    "test": "pnpm exec vitest run",
    "typecheck": "pnpm exec tsc --noEmit"
  },
  "dependencies": {
    "grab-bcv": "^0.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "esbuild": "^0.25.12",
    "oxlint": "^1.19.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
